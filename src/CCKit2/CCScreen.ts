/**
 * The CCScreen class represents a single display that holds windows. This is
 * usually the main terminal screen, but it could also be a monitor.
 * @category Windows
 */
export default class CCScreen {
    
}